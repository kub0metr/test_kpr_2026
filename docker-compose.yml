version: '3.8'

services:
  backend:
    build: ./BackApp
    ports:
    - "8000:8000"
    networks:
    - app-network

networks:
  app-network:
    driver: bridge
  # frontend:
  #   build: ./frontend